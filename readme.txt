1. git config --global user.name "Sam Smith"
2. git config --global user.emailsam@example.com
3. git add *   or git add file_name  # to add  file to stage
4. git status   # to see list of added stage
5. git commit -m "first commit"  # commit of what in stage for adding git repository
6. git remote add origin https://github.com/mahendradatascience/GitDemo.git  # for geting address of git repo
7. git push origin master  # final command to push all files and folder in git repo
8. git clone https://github.com/mahendradatascience/GitDemo.git  # to take all code in local repo. by usa employee to cheange 
9. git pull origin master  # to pull changed code with all by indian employee 